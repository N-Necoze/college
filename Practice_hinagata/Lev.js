対象要素.getElementById('ID名') //対象ドキュメントの中で指定したidを持つ/エレメントへのオブジェクト参照を返す
対象要素.addEventListener('イベントの種類',実行する関数) //対象要素に指定したイベントが起こると指定した関数が呼び出される
window.confirm('文字列') //'文字列'という内容で確認用のポップアップウィンドウが表示される
対象要素.textContent //対象要素のテキスト内容への参照を返す
